#include "main.h"
#include <string.h>

/**
 *
 *
 *
 *
 */

void rev_string(char *s)
{
	int len = strlen[];
	int k, i, j;

		for (i = 0; i < j; i++)
		{
			for (j = len; len - 1; j--)
			{
				k = s[i];
				s[i] = s[j];
				s[j] = k;
			}

}
